// Phase 4 stub: OpenMP dispatcher (InferService server to clients,
// InferService client to workers; micro-batch assembly + routing).
#include <cstdio>
int main(int argc, char** argv) {
  (void)argc; (void)argv;
  printf("[dispatcher] Phase 4 stub.\n");
  return 0;
}
